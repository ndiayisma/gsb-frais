<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Navbar with TailwindCSS</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body>
<nav class="bg-gray-800 p-4">
    <div class="container mx-auto flex justify-between items-center">
        <a href="{{ path('app_user_index') }}" class="text-white text-lg font-semibold">GSB</a>
        <div class="flex space-x-4">
            <a href="{{ path('app_fiche_frais') }}" class="text-gray-300 hover:text-white px-3 py-2">Fiche Frais</a>
            <a href="{{ path('app_fiche_frais_new') }}" class="text-gray-300 hover:text-white px-3 py-2">Saisie Fiche Frais</a>
            {% if app.user %}
                <a href="{{ path('app_logout') }}" class="bg-blue-500 text-white px-3 py-2 rounded">Logout</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="bg-blue-500 text-white px-3 py-2 rounded">Login</a>
            {% endif %}
        </div>
    </div>
</nav>


    {% block body %}
<div class="container mt-4"></div>
    {% endblock %}

</body>
</html>